CREATE TABLE  (
  `id int NOT NULL,
  `nombre` varchar(20) NOT NULL,
  `contraseña` varchar(30) NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=latin1;

INSERT INTO `alumno` (`nombre`, `contraseña`) VALUES
('Alejandra', 'hola');
